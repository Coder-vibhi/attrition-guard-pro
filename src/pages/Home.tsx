import Layout from '@/components/layout/Layout';
import HeroSection from '@/components/sections/HeroSection';

const Home = () => {
  return (
    <Layout>
      <HeroSection />
    </Layout>
  );
};

export default Home;